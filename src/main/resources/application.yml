spring:
  datasource:
    username: root
    password: 1234
    url: jdbc:postgresql://localhost:5432/necromancerfm
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
